<Window x:Name="mainWindow" x:Class="wpflab1.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:wpflab1"
        mc:Ignorable="d"
        Title="Mail Manager" Height="600" Width="800" MinWidth="800" MinHeight="400">
    <Grid Margin="0,0,0,0" MinWidth="800" MinHeight="400">
        <Grid.ColumnDefinitions>
            <ColumnDefinition MaxWidth="198" MinWidth="198"/>
            <ColumnDefinition MinWidth="2" Width="2"/>
            <ColumnDefinition MinWidth="600" Width="Auto"/>
        </Grid.ColumnDefinitions>
        <Grid Grid.Column="0">
            <TabControl>
                <TabItem Header="Received">
                    <ListBox Margin="0,0,0,0" x:Name="recvdListBox" HorizontalContentAlignment="Stretch" VerticalContentAlignment="Stretch"/>
                </TabItem>
                <TabItem Header="Sent" IsEnabled="True">
                    <ListBox Margin="0,0,0,0" x:Name="sentListBox" HorizontalContentAlignment="Stretch" VerticalContentAlignment="Stretch"/>
                </TabItem>
            </TabControl>
            
        </Grid>
        <Grid Grid.Column="1" Background="Gray"/>
        <Grid Grid.Column="2">
            <Grid.RowDefinitions>
                <RowDefinition Height="98"/>
                <RowDefinition MinHeight="2" Height="2"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <Grid Grid.Row="1" Background="Gray" Width="Auto"/>
            <Grid Grid.Row="0">
                <Button x:Name="loginButton" Content="Login" HorizontalAlignment="Left" Margin="10,10,0,0" VerticalAlignment="Top" Width="70" Height="70" Click="Button_Click" Grid.ColumnSpan="2" />
            </Grid>
        </Grid>
    </Grid>
</Window>
